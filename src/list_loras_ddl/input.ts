/**
 * No inputs — lists every LoRA the gateway exposes. (The image step's LoRA field
 * references this via @lookupOperation list_loras_ddl.)
 */
export type ListLorasDdlInput = Record<string, never>;
